apply plugin: 'groovy'
apply plugin: 'ivy-publish'

//sourceCompatibility = "1.7"
//targetCompatibility = "1.7"

group = 'plast-lab'
version = '1.0-SNAPSHOT'

task wrapper(type: Wrapper) {
    gradleVersion = '2.7'
}

repositories {
    mavenCentral()
    ivy {
        url "../ivy-repo"
    }
}

dependencies {
    compile "org.codehaus.groovy:groovy-all:2.3.10"            // Groovy
}

publishing {
    repositories {
        ivy {
            name "local-ivy-repo"
            url "../ivy-repo"
        }
    }

    publications {

        jar(IvyPublication) {
            organisation "$group"
            module "doop-persistent"
            revision "$version"
            from components.java
        }
    }
}
